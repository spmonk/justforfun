<?xml version="1.0" encoding="utf-8"?>
<resources>


    <style name="Default" parent="AlertDialog.AppCompat.Light">


        <item name="action_btn_positive_selector">@drawable/action_btn_positive_selector</item>
        <item name="action_btn_negative_selector">@drawable/action_btn_negative_selector</item>

        <!--Dialog window style-->
        <!--背景颜色及和透明程度-->
        <!--<item name="android:windowBackground">@android:color/transparent</item>-->
        <!--是否去除标题 -->
        <item name="android:windowNoTitle">true</item>
        <!--是否去除边框-->
        <item name="android:windowFrame">@null</item>
        <!--是否浮现在activity之上-->
        <item name="android:windowIsFloating">true</item>
        <!--是否模糊-->
        <item name="android:backgroundDimEnabled">true</item>
        <!--背景-->
        <item name="android:windowBackground">@drawable/dialog_background</item>
    </style>


    <style name="ActionButton">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">@dimen/dialog_action_button_height</item>
        <item name="android:focusable">true</item>
        <item name="android:layout_gravity">center</item>
    </style>

    <style name="ActionButton.Text">
        <item name="android:textSize">@dimen/dialog_action_button_text_size</item>
        <item name="android:textColor">@color/colorWhite</item>
        <item name="android:singleLine">true</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:gravity">center</item>
        <item name="android:minWidth">@dimen/dialog_action_button_min_width</item>
    </style>

</resources>